const SharedActivities = require('../models/sharedActitivies');
const Activities = require('../models/activity');


class SharedActivityController {
    async createSharedActivity(req, res) {
        
        //working on logic here.
        
    }
}